extends layout

block content
    body
        div(class='container')
            div(id='sideA')
                h2 Side A
                p #{cardData[0].sideA}
            script.
                function showHidden() {
                    document.getElementById('sideB').style.display=('block')
                    document.getElementById('nextCardLink').style.display=('block')
                    document.getElementById('nextCardLink1').style.display=('block')
                    document.getElementById('sideBbutton').style.display=('none')
                    }
                    
            button(href='#' id='sideBbutton' onclick='showHidden()') Show Side B
            div(id='sideB')
                h2 Side B
                p #{cardData[0].sideB}
                div 
            if category
                a(id='nextCardLink' href='/random/?category=' + category) Get a new card in the same category
                a(id='nextCardLink1' href='/random') Get a new random card
            else 
                a(id='nextCardLink' href='/random/') Get a new card